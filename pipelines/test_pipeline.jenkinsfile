@Library('shared-lib') _
pipeline {
    agent any
    stages {
        stage('Example Stage') {
            steps {
                script {
                    // 使用 runCmd 方法
                    runCmd('python --version')
                }
            }
        }
    }
}
